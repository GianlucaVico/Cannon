namespace Cannon_GUI
{
    /*
     * Colors representing player.
     * 
     * It is used both the GUI and the agent
     */
    public enum TileColor : byte
    {
        No,
        Dark,
        Light,
    }

}
